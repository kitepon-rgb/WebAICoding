# 記事レール広告の初期位置 — 仕様訂正と検証

- 訂正日: 2026-08-01
- 対象ページ: `https://kitepon.dev/blog/post/lattice-parallel-agents/`
- 訂正理由: 「フッターの分だけ上にずらす」を、ページ末尾だけ動的に退避する仕様へ誤って解釈していた

## 正しい受入条件

1. 広告レールは初期表示からフッター1個分だけ上へ表示する。
2. ページ先頭、途中、最下部で広告レールのviewport上の位置を変えない。
3. 最下部ではフッター上端と広告レール下端の間に通常の下余白を残す。
4. スクロールイベントで広告位置を更新しない。

## 実装

- CSSの`--rail-footer-offset`へ初回描画用の`6.875rem`を設定する。
- DOM構築後に実際のフッター高を計測し、左右レールへ共通で適用する。
- フッターの折り返しで高さが変わり得るresize時だけ再計測する。
- 旧実装のscroll listenerと、viewport下端からフッター上端までの動的距離計算を削除する。

## ローカル検証

viewport 1280×720、フッター実高109.875pxで確認した。

- 初回読込、`scrollY=0`: 左右レール下端590.125px、offset 109.875px
- ページ途中: 左右レール下端590.125px、offset 109.875px
- ページ最下部: 左右レール下端590.125px、offset 109.875px
- 最下部のフッター上端610.421875px、レールとの間隔20.296875px
- 広告は左右とも読込済み、console error 0件
- `hugo --minify`: 成功（174ページ）
- `node tools/validate-content.mjs --rendered`: 成功（posts=41、zenn=41、mobile=41）
